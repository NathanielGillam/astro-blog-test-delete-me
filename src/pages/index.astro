---
const clientname = "Polytechnix Pros";
const theYear = new Date();
const user = {
    name : 'Victor E. Eagle',
    age : 100,

}

const emojiSet = ['🍔', '🌭', '🍟', '🍕'];

import data from "../data/theData.json";


---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title> {clientname} </title>
	</head>
	<body>
		
		<a href="/">Home</a>
		<a href="/about/">About</a>

		<h1>{clientname}</h1>

		<p>EWU is now called {clientname}</p>
		<p>The mascot {user.name} is stored in theData {data.name} and is {user.age}</p>
		<p>{user.name} loves food. His favorites are:</p>
		<ul>
			{emojiSet.map(emoji => (
				<span>{emoji}</span>
			))}
		</ul>
		
		<footer>Copyright {theYear.getFullYear()}</footer>
	</body>
</html>
